{
  "name": "rules_v1_require_amount_match_fails",
  "description": "Offline verifier fails with rules_requirement_failed when rules-v1 requires exact settlement amount and evidence mismatches.",
  "requires": [
    "go test ./sdk/go/contractlane -run TestVerifySettlementProofV1_RulesRequireAmountMatchFails"
  ]
}
